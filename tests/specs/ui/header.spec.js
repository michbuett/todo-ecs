describe('Header', function () {
    'use strict';

    // var alchemy = require('');

    beforeEach(function () {
        setFixtures([
            '<section id="todoapp"></section>',
            '<footer id="info"></footer>',
        ].join(''));
    });

    it('allows to create new ToDos', function () {
        expect(true).toBeTruthy();
    });
});
